package com.example.marocship.helpers;

public interface Enum {

    enum status {PENDING, AFFECTED, DELIVERED}

    enum role {ADMIN, MANAGER, DELIVERY_MANAGER, DRIVER}

    enum vehicleType {CAR, TRUCK, BIG_TRUCK, PLANE}

}
